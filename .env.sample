# Copy to .env and edit for your environment

# For local use only:
OLLAMA_URL=http://192.168.0.179:11434

# For remote access from other devices, replace 127.0.0.1 with your machine's IP address:
# Example: OLLAMA_URL=http://192.168.1.100:11434
# (Find your IP with: ipconfig)

MODEL_NAME=llama3.2:3b
DESTINATIONS_DB_FILE=destinations.json
IMAGES_DIR=destination_images
# Use a writable path on your system (example for local dev)
DB_FILE=./data/aircraft.db
CHROMA_PERSIST_DIR=chroma_db
EMBEDDING_MODEL=all-MiniLM-L6-v2
CHROMA_COLLECTION_NAME=wikivoyage_travel
